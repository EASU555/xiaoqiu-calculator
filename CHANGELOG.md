# v1.3.4 — 2026-07-28

- 重新整理桌面端字体层级，标题、分区标题、标签、正文和说明文字采用更协调的 `20 / 13 / 12 / 11 / 10` 字号梯度。
- 输入数字提升至 `15`，结果数字提升至 `22`，按钮与历史结果同步放大，增强高 DPI 下的可读性。
- 同步放大主题图标、应用标记和快捷计数主按钮文字。
- 完成浅色、深色、基础运算、快捷计数及 `440×400` 最小窗口检查，未出现文字裁切或横向溢出。

# v1.3.3 — 2026-07-28

- 修复空历史记录区域持续上下抖动的问题；根因是滚动条显隐与尺寸监听互相触发。
- 移除依赖实时控件高度的自动显隐循环，滚动条改为固定占位，不再改变历史面板布局。
- 空历史和单条历史时将滑块融入背景，两条以上记录时再显示滑块，不影响滚动操作。
- 新增与控件几何尺寸无关的滚动条规则回归测试。

# v1.3.2 — 2026-07-28

- 精修浅色与深色界面，移除容易产生灰色毛边的偏移假阴影，改用单层面板与轻量色阶表达层级。
- 统一面板、输入框、选择器和按钮的圆角、描边与悬停状态，减少重复橙色轮廓。
- 收敛橙色使用范围，让主按钮、当前模式和关键结果保持清晰的视觉优先级。
- 将滚动条改为圆角窄滑块，并在内容无需滚动时自动隐藏；最小窗口和长历史下仍可正常滚动。
- 修正快捷计数页的说明文字对齐，并保持现有键盘路径、窗口缩放和高 DPI 行为。

# v1.3.1 — 2026-07-28

- 新增标题栏浅色/深色切换按钮；浅色保持当前灰白模板，深色恢复旧版石墨黑与橙色模板。
- 两种主题共用现有布局、状态语义和键盘路径，切换时不会丢失输入、结果、历史或计数。
- 外观模式会独立保存并在下次启动时恢复；损坏或未知的外观设置会安全回退到浅色。
- Windows 原生标题栏会与应用主题同步切换明暗。
- 调整顶部导航密度，确保新增主题按钮在 `440×400` 最小窗口和高 DPI 下不遮挡标题。
- 新增外观设置读写、异常回退和旧深色调色板回归测试。

# v1.3.0 — 2026-07-28

- Windows 界面改为浅灰白背景、白色工作面板、柔和阴影和橙色主操作按钮。
- 双结果、乘加和基础运算页面统一为“上方历史记录、下方输入/运算/紧凑结果”的桌面工作台布局。
- 点击计算按钮或按 Enter 完成有效计算后写入历史；输入过程中的实时预览不会写入历史。
- 三种计算模式分别保存最多 50 条历史，自动写入用户数据目录并在重启后恢复。
- 增加明确的“清空历史”操作和 Windows 确认对话框；清空当前模式不会影响其他模式。
- 快捷计数页调整为上方计数数字、下方醒目大号 `+1` 和次要“清零”按钮。
- `Space` 现在始终可用于计数，同时继续支持原有可保存的自定义快捷键。
- 增加可缩放和高 DPI 布局修复；小窗口改用页面内部滚动，避免控件遮挡、溢出或裁切。
- 新增历史上限、模式隔离、持久化恢复、独立清空和 Space/自定义快捷键并存测试。

# v1.2.0 — 2026-07-23

- 将乘加模式的固定系数 `475` 改为可自由调节，默认值仍为 `475`。
- 系数输入支持整数、小数和负数，并加入独立的空值、非法格式与超大结果校验。
- 公式预览、结果状态、键盘焦点路径和清空逻辑会与当前系数同步；清空时保留有效系数。
- 新增独立“基础运算”页面，支持固定值与运算值进行加、减、乘、除，运算符一次只能选择一个。
- 固定值默认 `475` 且可修改；支持整数、小数、负数、除零提示和两位小数显示规则。
- 顶部改为“计算器 / 基础运算 / 快捷计数”三页面导航，并补齐 Enter、Esc、Tab 与方向键操作路径。
- 双结果、乘加计算和基础运算现在都会随输入实时刷新，切换基础运算符时也会立即更新结果。
- 空值和未完成数字在输入阶段保持安静的 `—` 状态，计算按钮与 Enter 继续提供显式错误校验。

# v1.1.0 — 2026-07-22

- 新增固定 `475 × 乘数 + 加数 = 结果` 计算模式，支持整数、小数、负数和两位小数结果规则。
- 新模式与原双结果模式使用独立输入、结果和状态，切换后数据互不覆盖。
- 新增快捷计数器，每次点击 `+1` 数字增加一。
- 将快捷计数器拆分为独立页面，并在窗口顶部增加“快捷计数 / 返回计算”切换按钮。
- 计算快捷键与计数快捷键按页面隔离；切换页面时保留输入、结果、计数值和绑定设置。
- 计数快捷键默认为 `Space`，现在可在界面中自由绑定并自动保存。
- 修复部分中文输入法、特殊键盘布局和数字小键盘按键被误判为不可用的问题；改用键名与 Windows 按键码双重匹配。
- 输入框聚焦时不会误触计数；回车、Esc、Tab 和单独的修饰键保留给原有操作。
- 新增独立“清零”按钮。
- 增加长计数值字号适配、快捷键校验、设置读写和计数逻辑回归测试。

# v1.0.0 — 2026-07-17

首个公开版本。

## 功能

- 输入 A、B，同时计算 `A + B` 与 `B ÷ 自定义除数`。
- 除数默认 475，可修改为其他整数或小数。
- 结果四舍五入到 2 位，并省略无意义的末尾 0。
- 支持回车计算、Esc 清空和完整键盘焦点路径。

## 稳定性与安全修复

- 修复超长数字和极小除数触发 `decimal.InvalidOperation` 的崩溃路径。
- 增加输入长度、结果长度和除零保护。
- 使用高精度 Decimal 计算上下文，并为长结果自动调整字号。
- 固定 Python 与 GitHub Actions 依赖版本。
- 添加 8 项边界与回归测试，以及 Windows 自动构建流程。
- 依赖漏洞扫描结果：未发现已知漏洞。

## Windows 可执行文件

- 文件：`ABCalculator.exe`
- SHA-256：`6A4DD5DBC92ABB9DC775F2B9EFF463FD101CC163CCD3F7DBCDB76E7BE985EA1F`
